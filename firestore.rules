rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    /* Solo la app autenticada (login anónimo de la propia app)
       puede leer/escribir. Bloquea acceso externo. */
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}